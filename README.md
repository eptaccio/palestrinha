# palestrinha

# Running

## docker-compose
> docker-compose build  
> docker-compose up -d

## local install
> create a .env (see a example in `.env.example`)  
> npm install   
> npm start

# running tests
> npm run test

